name: Pull Request Test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # You might need to set up your environment here, like installing dependencies or setting up databases.

      - name: Run tests
        run: npm run test