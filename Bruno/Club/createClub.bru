meta {
  name: createClub
  type: http
  seq: 1
}

post {
  url: http://localhost:4000/clubs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Club Name"
  }
}
